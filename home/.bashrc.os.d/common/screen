# vim: filetype=sh:

export BASHRC_SCREEN_CONF_DIR="$HOME/.screen/configs"
BASHRC_SCREEN_CONF="${BASHRC_HOST_CONFIG}.screenrc"  # -- or whatever you want

if [ ! -f  "${SCREEN_CONF_DIR}/${SCREEN_CONF}" ]
then
  BASHRC_SCREEN_CONF="default.screenrc"
fi

export BASHRC_SCREEN_CONF
